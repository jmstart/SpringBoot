server:
  port: 8088
  servlet:
    path: "*.do"
logging:
  level:
    com.jiaming: debug
    # org.springframework: debug

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/springbootdemo
    username: root
    password: root

mybatis:
  #驼峰 通用mapper 也引入了
  #configuration:
    #map-underscore-to-camel-case: true
  type-aliases-package: com.jiaming.pojo
  # mapper-locations: mapper/*.xml
